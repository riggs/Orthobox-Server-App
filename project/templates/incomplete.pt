<html>
<head>
    <style type="text/css">
        body {
            background-color: #f3f3f3;
            margin: 20px;
        }

        h2, h3, h3 a, p {
            font-family: Helvetica Neue Light, Helvetica, Arial;
            color: #414141;
        }

        h3 {
            font-family: Helvetica Neue Bold, Helvetica, Arial;
            color: #FFF;
            font-weight: bold;
            display: inline-block;
            padding: 30px;
            background-color: #e3c225;
            text-align: center;
            font-size: 300%;
        }

        #inner {
            margin: auto;
            text-align: center;
            width: 60%;
            height: 60%;
        }
    </style>
</head>
<body>
<div id="inner">
    <h2>${activity}</h2>

    <h3>I</h3>

    <p>Try again, ${username}.</p>

    <div tal:switch="version">

        <p tal:case="None">You completed your first trial of the <strong>${activity}</strong> task.
            <br/>You received an
            <strong>incomplete</strong> score. You took <strong>${duration}</strong> seconds and made
            <strong>${error_number}</strong> errors.</p>

        <p tal:case="'peggy_dev'">You completed your first trial of the <strong>${activity}</strong> task. <br/>You
            received an
            <strong>incomplete</strong> score. You took <strong>${duration}</strong> seconds and made
            <strong>${error_number}</strong> errors.</p>

        <p tal:case="'pokey_dev'">You completed your first trial of the <strong>${activity}</strong> task. <br/>You
            received an
            <strong>incomplete</strong> score. You completed <strong>${pokes}</strong> pokes in
            <strong>${duration}</strong>
            seconds and made
            <strong>${error_number}</strong> errors.</p>
    </div>
    <h3><a href="${video_url}">View Video</a></h3>
</div>
</body>
</html>
